{
  "sql": "CREATE TABLE orders (id SERIAL PRIMARY KEY, user_id INTEGER REFERENCES users(id), total NUMERIC(10,2), status VARCHAR(50) DEFAULT 'pending', created_at TIMESTAMP DEFAULT NOW());",
  "sourceDb": "postgresql"
}
